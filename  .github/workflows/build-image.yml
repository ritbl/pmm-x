name: Docker Image CI

on:
  push:
    branches: [ "amd64" ]
  pull_request:
    branches: [ "FB/*" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: build
        run: make build-amd64
